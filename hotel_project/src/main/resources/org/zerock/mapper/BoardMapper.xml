<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.zerock.mapper.BoardMapper">



	
	
	<select id="imageList" resultType="org.zerock.domain.MainVO">
			
				
		<![CDATA[
	
select t.* from (select n.hotelFileName,rh.realhotelname, r.grade,r.reviewContent, r.reviewDate from hotel n,review r,realhotel rh

where n.hotelNo=r.hotelNo and n.realhotelno = rh.realhotelno order by r.grade desc) t where rownum<5
		
	
	
		]]>


	</select>

	<select id="myPageList" resultType="org.zerock.domain.MyPageVO">			
		<![CDATA[
	
	select m.memberNo, m.memberName, m.memberId, m.memberNickname, r.roomNo, r.reservationNo, r.userAmount, r.realUser, r.checkInDate, r.checkOutDate, h.hotelName, h.mainAddress, n.reviewContent, n.reviewDate, i.hotelImageId, i.hotelFilename, w.wishListNo
	from member m, reservation r, hotel h, review n, hotel_image i, wishList w
	where m.memberNo = r.memberNo 
		and r.memberNo = n.memberNo 
		and n.memberNo = w.memberNo
		and h.hotelNo = r.roomNo 
	    and h.hotelNo = n.hotelNo
	    and h.hotelNo = i.hotelNo
	    and i.hotelNo = w.hotelNo
		and m.memberNo = 1
		]]>
	</select>


</mapper>
